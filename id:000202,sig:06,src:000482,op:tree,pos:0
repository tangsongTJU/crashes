function Inner() {
    Inner()
}

var foo = function() {
    var inner = new Inner();
    var done = false;
    inner.isDone = function() {
        done = true;
    }

    while (function() {
        done = true;
    }) {
        var val = inner.doStuff();
        if (done)
            break;
    }
}

foo();
