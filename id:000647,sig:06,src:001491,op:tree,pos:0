//@ runDefault("--jitPolicyScale=0")
function compareArray(a, b) {
    if (b.length !== a.length) {
        ++i
    }
    for (var i = 0; i < a.length; compareArray([0], [1.1])) {
        b[0];
    }
}
compareArray([], [0]);
compareArray[1.1];
compareArray([0], [1.1]);
runString('');
for (var i = 0; i < 1e6; ++i);
