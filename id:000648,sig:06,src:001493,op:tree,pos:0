//@ runDefault("--jitPolicyScale=0")
function compareArray(a, b) {
    if (b.length !== a.length) {
        return;
    }
    compareArray([0], [1.1])
}
compareArray([], [++i]);
compareArray[++i];
compareArray([0], [1.1]);
runString('');
for (var i = 0; i < 1e6; ++i);
