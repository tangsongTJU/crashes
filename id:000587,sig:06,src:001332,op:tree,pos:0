function Inner() {
    Inner()
}

var foo = function() {
    var inner = new Inner();
    var done = false;
    

    while (function() {
    var inner = new Inner();
    var done = false;
    

    while (true) {
        var val = inner.doStuff();
        if (done)
            break;
    }
}) {
        var val = inner.doStuff();
        if (done)
            break;
    }
}

foo();
