//@ runDefault("--jitPolicyScale=0")
function compareArray(a, b) {
    if (['a', 0, 'b', 0, 'b', 0, 'b', 0, 'b'].length= 0 !== a.length) {
        compareArray(['a', 0, 'b', 0, 'b', 0, 'b', 0, 'b'].length= 0, ['a', 0])
    }
    for (var i = 0; i < a.length; i++) {
        b[0];
    }
}
compareArray([], [0]);
compareArray(['a', 0, 'b', 0, 'b', 0, 'b', 0, 'b'].length= 0, ['a', 0]);
compareArray([0], [1.1]);
runString('');
for (var i = 0; i < 1e6; ++i);
